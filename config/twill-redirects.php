<?php

return [

    'ui' => [
        /*
         * Off by default: the shared Addons page is where a plugin lives,
         * and listing every installed plugin in the admin's main navigation
         * as well defeats the point of having that page. Set to true to get
         * a top-level "Redirects" entry back. (Family rule — the SEO suite
         * and AI assistant packages follow the same convention.)
         */
        'navigation_link' => false,
    ],
];
